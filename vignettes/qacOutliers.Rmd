---
title: "qacOutliers"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{univOutliers}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(qacOutliers)
```

### What are outliers? 
- describe what they are, provide other common names for them (anamolies, etc.)

### Why should we detect outliers?
- some basic reasons to get rid of them 

### How can you detect outliers? 

- provide links to each of the seven methods, explain that it differs for univariate and multivariate outliers

### What should you do with outliers once you detect them? 

- up to you, decide if you want to remove them or continue analysis with them 
